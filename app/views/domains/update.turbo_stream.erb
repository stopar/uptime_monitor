<% # Below is no longer required as it will be triggered from models/domain.rb once the Monitor/Stop Monitoring button is click %>
<% # Replace Monitoring %>
<% # <%= turbo_stream.update "monitoring", partial: "domains/monitoring", locals: { domain: @domain} %>


<% # Show flash message %>
<%= turbo_stream.prepend "flash", partial: "layouts/flash" %> 
